# Ingestion Instructions (Azure AI Foundry)

1. Create a new **Data** asset (Files).
2. Upload the `rag/<pack>/docs` folder.
3. In your **Chat Agent**, add a **Retrieval** tool referencing the Data asset.
4. Optional: Enable **citations** and **semantic reranking**.
